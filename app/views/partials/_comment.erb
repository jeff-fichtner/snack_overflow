<div class='comment' id='<%= comment.id %>'>
  <%= comment.body %>
  <div class='comment footer'>
    <%= comment.user.username %> wrote this comment at <%= comment.created_at %>
  </div>
</div>
<br>
